<br><%= render partial: "s_errors" %>

<%= form_with model: @stylist, local: true do |f| %>
    <%= f.label :name, "Stylist Name: " %>
    <%= f.text_field :name %><br><Br>

    <%= f.submit %>
<% end %>